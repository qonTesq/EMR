# EMR Database Configuration
# Rename this file to ".env" and update with your actual database credentials

# Database Connection URL
# Format: jdbc:mysql://<host>:<port>/<database_name>
# Example: jdbc:mysql://localhost:3306/emr_db
EMR_DB_URL=jdbc:mysql://localhost:3306/emr_db

# Database Username
# Default: root (for local development)
EMR_DB_USER=root

# Database Password
# Leave empty for local development or set your secure password
EMR_DB_PASSWORD=

# ========================================
# Notes:
# ========================================
# 1. Never commit the actual .env file to version control
# 2. Add .env to your .gitignore file
# 3. For production, use strong passwords and secure connection strings
# 4. Ensure MySQL server is running before starting the application
# 5. Create the emr_db database before first run:
#    mysql -u root -p
#    CREATE DATABASE emr_db;
